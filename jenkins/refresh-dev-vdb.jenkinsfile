pipeline {
    agent any
    stages {
        stage("refresh DEV from last snapshot") {
            steps {
                sh '''
                    cd rnacentral
                    fab localhost refresh_dev
                '''
            }
        }
    }
}