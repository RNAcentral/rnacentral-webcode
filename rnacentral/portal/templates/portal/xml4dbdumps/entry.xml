<entry id="{{ rna.upi }}">
	<name>{{ rna.upi }}</name>
    <description>Unique RNA sequence from {{ rna.count_distinct_organisms }} organism{{ rna.count_distinct_organisms|pluralize }} with {{ rna.count_xrefs }} annotation{{ rna.count_xrefs|pluralize }} from {{ rna.count_distinct_databases }} database{{ rna.count_distinct_databases|pluralize }}</description>
    <cross_references>
   		{% spaceless %}
    		{% for xref in xrefs %}
    			{% if xref.accession__non_coding_id %}
					<ref dbkey="{{ xref.accession__external_id }}" dbname="{{ xref.db__descr }}" />
				{% else %}
					<ref dbkey="{{ xref.accession__accession }}" dbname="{{ xref.db__descr }}" />
				{% endif %}
			{% endfor %}
    		{% for taxid in rna.get_distinct_taxids %}
				<ref dbkey="{{ taxid }}" dbname="ncbi_taxonomy_id" />
			{% endfor %}
		{% endspaceless %}
    </cross_references>
    <dates>
        <date value="{{ rna.first_seen }}" type="first_seen" />
        <date value="{{ rna.last_seen }}" type="last_seen" />
    </dates>
    <additional_fields>
        <field name="active">{{ rna.is_active }}</field>
        <field name="length">{{ rna.length }}</field>
        {% spaceless %}
        	{% for species in rna.get_distinct_species %}
        		<field name="species">{{ species }}</field>
        	{% endfor %}
        {% endspaceless %}
        {% spaceless %}
        	{% for expert_db in rna.get_distinct_expert_dbs %}
        		<field name="expert_db">{{ expert_db }}</field>
        	{% endfor %}
        {% endspaceless %}
        {% spaceless %}
        	{% for organelle in rna.get_distinct_organelles %}
				<field name="organelle">{{ organelle }}</field>
        	{% endfor %}
        {% endspaceless %}
    </additional_fields>
</entry>
