apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    pvc: rnacentral-species-specific-ids
  name: rnacentral-species-specific-ids
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
